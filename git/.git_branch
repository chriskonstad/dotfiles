git -C $(tmux display-message -p -F "#{pane_current_path}" -t0) rev-parse --abbrev-ref HEAD 2>&1 | grep -v "fatal: *" | grep -v "Stopping at filesystem boundary (GIT_DISCOVERY_ACROSS_FILESYSTEM not set)."
